package SeleniumWebDriverAssignment;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.testng.annotations.BeforeSuite;
import org.testng.annotations.Test;

public class LoginWithCorrectDetailsClient {

	WebDriver driver;
	LoginWithCorrectDetailsClient lwcd;

	@BeforeSuite

	public void setUp() {

		driver = new ChromeDriver();
		driver.manage().window().maximize();

		lwcd = new LoginWithCorrectDetailsClient(driver);

	}

	@Test(priority = 1)
	public void login() throws InterruptedException {
		lwcd.url();
		lwcd.login("darshankhuba0@gmail.com", "Likith@123");
		lwcd.closeBrowser();
	}

}